# Development / CI tooling. Not required to run PetitePass.
-r requirements.txt
pytest==9.0.3
ruff==0.9.2
pip-audit==2.7.3
mypy==1.14.1
# Used by `make build` to produce the standalone binary.
pyinstaller==6.11.1
